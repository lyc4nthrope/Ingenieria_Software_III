/**
 * Router Configuration
 * 
 * Define todas las rutas de la aplicación usando React Router
 * Agrupa rutas por modules (auth, publications, orders, delivery)
 */

import React from 'react';

/**
 * Configuración de rutas de la aplicación
 * 
 * Estructura por procesos de negocio:
 * - /auth/* - Autenticación y gestión de usuario
 * - /publications/* - Gestión de publicaciones (cuando esté implementado)
 * - /orders/* - Gestión de pedidos (cuando esté implementado)
 * - /delivery/* - Gestión de entregas (cuando esté implementado)
 */

export const routerConfig = [
  // Las rutas irán aquí
  // Ejemplo:
  // {
  //   path: '/',
  //   element: <Layout />,
  //   children: [
  //     { path: 'auth/login', element: <LoginPage /> },
  //     // ...
  //   ]
  // }
];

export default routerConfig;
